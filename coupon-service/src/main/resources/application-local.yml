spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookmyshow_coupon
    username: root
    password: rootpassword

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
